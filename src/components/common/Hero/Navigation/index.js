import {Link, useLocation} from 'react-router-dom';import Button from '../../../ui/Button';import logoSrc from '../../../../assets/images/logo.png';const Navigation = ({commentsRef}) => {  const location = useLocation();  const activeItemStyles = 'line-through text-primary';  const itemStyles = 'font-medium hover:opacity-90 duration-200';  const handleScroll = () => {    commentsRef.current.scrollIntoView({ behavior: 'smooth' });  };  return (    <div className="max-w-[1200px] min-h-[170px] ml-auto mr-auto flex flex-col justify-center items-center text-white gap-4 px-4 py-8 md:py-0 md:flex-row md:justify-between">      <Link to="/">        <img          src={logoSrc}          alt="Logo"        />      </Link>      <div className="flex flex-col md:flex-row items-center gap-4">        <Link          to="/"          className={`${itemStyles} ${location.pathname === '/' ? activeItemStyles : ''}`}        >          Home        </Link>        <Link          to="/services"          className={`${itemStyles} ${location.pathname === '/services' ? activeItemStyles : ''}`}        >          Services        </Link>        <Link          to="/experience"          className={`${itemStyles} ${location.pathname === '/experience' ? activeItemStyles : ''}`}        >          Experience        </Link>        <Button          text="Comments"          onClick={handleScroll}        />      </div>    </div>  )}export default Navigation;