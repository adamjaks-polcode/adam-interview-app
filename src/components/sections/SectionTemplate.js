const SectionTemplate = (  {    mode = 'darken',    title = '',    subtitle = '',    children  }) => {  const backgroundColor = mode === 'darken' ? 'bg-background-darken' : 'bg-background-lighten';  return (    <div className={`${backgroundColor} py-24 md:py-32 px-4`}>      <div className="max-w-[1200px] ml-auto mr-auto text-white text-center">        <h3 className="text-primary font-bold uppercase text-2xl">{subtitle}</h3>        <h2 className="text-3xl font-semibold mb-9">{title}</h2>        { children }      </div>    </div>  )}export default SectionTemplate;