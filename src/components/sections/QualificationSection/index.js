import QualificationSectionItem from './QualificationSectionItem';import SectionTemplate from '../SectionTemplate';const QualificationSection = (  {    items = []  }) => {  return (    <SectionTemplate      title="My Experience"      subtitle="Qualification"      mode="lighted"    >      <p className="mt-6">It is a long established fact that a reader will be distracted by the readable lets content of a page into.</p>      <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mt-20">        {          items.map(item => (            <QualificationSectionItem              data={item}              key={item.id}            />          ))        }      </div>    </SectionTemplate>  )}export default QualificationSection;