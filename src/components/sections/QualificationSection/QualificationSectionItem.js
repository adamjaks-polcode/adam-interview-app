import moment from 'moment';const QualificationSectionItem = ({data}) => {  const { company, description, from, to, id } = data;  const imageUrl = require(`../../../assets/images/qualification/${id}.png`);  return (    <div className="text-left shadow-lg p-6 bg-background-lighten">      <div className="flex items-center">        <div className="w-[40px] h-[40px] bg-primary flex items-center justify-center">          <img            src={imageUrl}            alt={`Icon: ${company}`}          />        </div>        <div className="ml-9">          <h5 className="">{company}</h5>          <p className="text-grey-medium">            { moment(from).format("MMMM yyyy")} - { moment(to).format("MMMM yyyy")}          </p>        </div>      </div>      <h4 className="text-lg font-bold mt-6">Print & Web Designer</h4>      <p className="text-grey-light">{description}</p>    </div>  )}export default QualificationSectionItem;