import WhatWeDoSectionItem from './WhatWeDoSectionItem';import SectionTemplate from '../SectionTemplate';const WhatWeDoSection = (  {    items = []  }) => {  return (    <SectionTemplate      title="Better Services For You"      subtitle="What we do"    >      <p>It is a long established fact that a reader will be distracted by the readable lets content of a page into.</p>      <div className="flex flex-col md:flex-row gap-4 mt-20">        {          items.map(item => (            <WhatWeDoSectionItem              data={item}              key={item.id}            />          ))        }      </div>    </SectionTemplate>  );};export default WhatWeDoSection;